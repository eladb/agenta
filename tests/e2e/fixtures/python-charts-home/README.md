agenta e2e home (python-charts fixture)

This home is used by `tests/e2e/skills-golden.test.ts` to exercise the
skills-projection path. It carries one skill — `python-charts` — that
instructs the agent to render a chart with matplotlib and share it via
the `share_file` tool.
